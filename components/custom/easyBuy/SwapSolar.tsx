import React, { FunctionComponent } from "react";
import { StyleSheet, View } from "react-native";

interface SwapSolarProps {}

const SwapSolar: FunctionComponent<SwapSolarProps> = ({}) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default SwapSolar;
